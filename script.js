document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open);});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false');}));}const canvas=document.getElementById('orbit-canvas');if(canvas){const ctx=canvas.getContext('2d'),dpr=Math.min(devicePixelRatio||1,2);let w,h,t=0;function resize(){const r=canvas.getBoundingClientRect();w=r.width;h=r.height;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}function draw(){ctx.clearRect(0,0,w,h);const cx=w/2,cy=h/2;ctx.save();ctx.translate(cx,cy);[.26,.38,.5,.62,.74].forEach((scale,i)=>{ctx.beginPath();ctx.ellipse(0,0,w*scale,h*scale*.55,(i%2-.5)*.45,0,Math.PI*2);ctx.strokeStyle=`rgba(241,244,238,${.12-i*.014})`;ctx.lineWidth=1;ctx.stroke()});const a=t*.65;const x=Math.cos(a)*w*.31,y=Math.sin(a)*h*.18;ctx.beginPath();ctx.arc(x,y,7,0,Math.PI*2);ctx.fillStyle='#f3cc6a';ctx.fill();ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(x,y);ctx.strokeStyle='rgba(115,215,245,.35)';ctx.stroke();ctx.beginPath();ctx.arc(0,0,12+Math.sin(t*2)*2,0,Math.PI*2);ctx.fillStyle='#73d7f5';ctx.fill();ctx.restore();t+=.008;requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();}});